spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
#spring.flyway.enabled=false

application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=3600000
application.security.jwt.refresh-token.expiration=86400000

# Mock Cloudinary for tests
storage.cloudinary.cloud-name=test
storage.cloudinary.api-key=test
storage.cloudinary.api-secret=test

# Paystack
paystack.secret=test
paystack.url=http://localhost:8080

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Mail
spring.mail.host=localhost
spring.mail.port=25
spring.mail.username=test

# OAuth2 (dummies for context load)
spring.security.oauth2.client.registration.google.client-id=test-id
spring.security.oauth2.client.registration.google.client-secret=test-secret
application.oauth2.authorized-redirect-uris=http://localhost:3000/oauth2/redirect

# Disable health checks for components we mock
management.health.redis.enabled=false
management.health.mail.enabled=false

# Admin seed
perfume.budget.admin.email=admin@test.com
perfume.budget.admin.password=password

frontend.change-password-url=http:localhost:8080/change-password
